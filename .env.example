# Copy to .env and fill locally — never commit .env (see .gitignore).
# Used by: node tools/push-availability.mjs
# PowerShell:  $env:UPDATE_JSON_KEY = "your_token"
# Bash:         export UPDATE_JSON_KEY=your_token

UPDATE_JSON_KEY=

# Optional: avoid passing owner/repo on the command line
# GITHUB_OWNER=rahellyg
# GITHUB_REPO=NAOMI
# GITHUB_BRANCH=main
# GITHUB_FILE=naomi-availability.json
